{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "env": {
    "DATABASE_URL": {
      "description": "PostgreSQL database connection string (from Vercel Postgres)"
    },
    "NEXTAUTH_SECRET": {
      "description": "Random secret for NextAuth.js (generate with: openssl rand -base64 32)"
    },
    "NEXTAUTH_URL": {
      "description": "Full URL of your deployed application (e.g., https://your-app.vercel.app)"
    }
  }
}
